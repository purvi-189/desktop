import { ENV } from '../constants';
export declare type GetSubscribersOptionsType = {
    channel: string;
    env?: ENV;
};
export declare const _getSubscribers: (options: GetSubscribersOptionsType) => Promise<any>;
