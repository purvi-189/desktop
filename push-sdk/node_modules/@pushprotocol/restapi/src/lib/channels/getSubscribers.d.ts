import { ENV } from '../constants';
import { Subscribers } from '../types';
/**
 *  GET /v1/channels/:channelId/:subscribers
 */
export declare type GetChannelSubscribersOptionsType = {
    channel: string;
    page?: number;
    limit?: number;
    env?: ENV;
};
export declare const getSubscribers: (options: GetChannelSubscribersOptionsType) => Promise<Subscribers>;
