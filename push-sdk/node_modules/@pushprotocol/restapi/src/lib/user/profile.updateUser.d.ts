import { ENV } from '../constants';
import { IUser, ProgressHookType } from '../types';
declare type ProfileUpdateProps = {
    /**
     * PGP Private Key
     */
    pgpPrivateKey: string;
    /**
     * DID
     */
    account: string;
    /**
     *  Profile properties that can be changed
     */
    profile: {
        name?: string;
        desc?: string;
        picture?: string;
    };
    env?: ENV;
    progressHook?: (progress: ProgressHookType) => void;
};
/**
 * Updation of profile
 */
export declare const profileUpdate: (options: ProfileUpdateProps) => Promise<IUser>;
export {};
