import { ENV } from '../../constants';
import { SignerType, VideoCallStatus } from '../../types';
interface VideoCallInfoType {
    recipientAddress: string;
    senderAddress: string;
    chatId: string;
    signalData: any;
    status: VideoCallStatus;
    env?: ENV;
}
interface UserInfoType {
    signer: SignerType;
    chainId: number;
    pgpPrivateKey: string;
}
declare const sendVideoCallNotification: ({ signer, chainId, pgpPrivateKey }: UserInfoType, { recipientAddress, senderAddress, chatId, signalData, status, env, }: VideoCallInfoType) => Promise<void>;
export default sendVideoCallNotification;
