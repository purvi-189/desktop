import { ENV } from '../../constants';
import { IConnectedUser, IUser, walletType } from '../../types';
export declare const createUserIfNecessary: (wallet: walletType, env: ENV) => Promise<IUser>;
export declare const getConnectedUser: (wallet: walletType, privateKey: string | null, env: ENV) => Promise<IConnectedUser>;
export declare const getConnectedUserV2: (wallet: walletType, privateKey: string | null, env: ENV) => Promise<IConnectedUser>;
export declare const getUserDID: (address: string, env: ENV) => Promise<string>;
